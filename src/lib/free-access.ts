/**
 * Free-access period.
 *
 * While this is true, every Relay Pro feature is unlocked for everyone and the
 * paid checkout is hidden. Flip it to false to bring back paid upgrades —
 * nothing else needs changing (existing purchases are still honoured).
 */
export const FREE_ACCESS = true;
